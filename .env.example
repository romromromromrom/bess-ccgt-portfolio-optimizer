# Copy to .env (gitignored) and fill in locally, or better: use an EC2
# instance role in production so no key ever needs to exist as a file.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=eu-west-3
S3_BUCKET=
