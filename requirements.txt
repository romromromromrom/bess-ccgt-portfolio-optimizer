# Dependency list for deployment targets that install with pip and do not build
# the project itself - Streamlit Community Cloud in particular, which otherwise
# picks up pyproject.toml, drives it with Poetry, and fails because Poetry does
# not read the setuptools src-layout configuration.
#
# pyproject.toml remains the source of truth for local development, Docker and
# CI (pip install -e ".[dev]"). Keep the two in sync when adding a dependency.
pandas>=2.0
pydantic>=2.0
pyomo>=6.7
highspy>=1.7
streamlit>=1.35
altair>=5.0
boto3>=1.34
pyyaml>=6.0
